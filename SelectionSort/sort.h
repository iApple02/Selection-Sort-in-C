#ifndef SORT_H
#define SORT_H

#include "logging.h"

void selectionSort(int*, int, float, LinkedList*);
void animateArray(int*, int, int, int, int, float);
void animateSwap(int*, int, int, int, float);
void freeEntry(void*);

#endif
